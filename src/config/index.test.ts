import { expect } from 'chai';

import { MODE, MONGO_URL } from './index';

// describe('MODE', function() {
//   it("should default to 'test'", function() {
//     const temp = process.env.MODE;
//     process.env.MODE = undefined;
//     expect(MODE).to.equal('test');
//     process.env.MODE = temp;
//   });
//   it("should be overriden by the 'MODE' environment variable", function() {
//     const testString = 'n6agfa7gf';
//     const temp = process.env.MODE;
//     process.env.MODE = testString;
//     expect(MODE).to.equal(testString);
//     process.env.MODE = temp;
//   });
// });
